[pytest]
addopts = -p no:warnings
# Definition of custom markers
markers=
   # State markers
   ready: Tests that are ready for non regression
   serial: Tests that cannot, in any way, be executed in parallel
   # Yapily e2e tests
   yapily-ee: Yapily end to end tests
   encryption: Encryption tests
   blacklist_service: Blacklist services tests